// Match mkd app breakpoints
export const BP_DESKTOP = 1110; // above = full sidebar visible; below = slide-out + optional icon bar
export const BP_TABLET = 641;   // 641–1110 = tablet (icon bar); below = mobile (hamburger only)
