# Copy to .env and fill in values. Never commit .env.

# Optional: API base URL (defaults to http://localhost:3001 when not set)
# VITE_API_URL=http://localhost:3001

# Production: set VITE_API_URL in Netlify (Site settings → Environment variables)
# or in netlify.toml - use your deployed backend URL, not localhost.